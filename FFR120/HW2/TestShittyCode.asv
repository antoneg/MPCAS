locationStatus = [1,0; 2,0; 3,1; 4,1; 5,1; 6,1; 7,1; 8,0; 9,0];
gridSize = 3;

cluster = [];

figure(1)
for i = 1:size(locationStatus,1)
    loc  = locationStatus(i,1);
    row = ( ceil(loc/gridSize));
    col = loc -((row-1)*gridSize);
   
   if locationStatus(i,2) == 1
    plot(row, col, 'go')
    hold on
    text()
    hold on
   end
end
hold off
xlim = ([0 4]);
ylim = ([0 4]);


 cluster = BurnNeighbours(locationStatus, 5, 3);
 cluster